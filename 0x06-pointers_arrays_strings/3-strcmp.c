#include "main.h"
/**
 * _strcmp - compare two string
 * @s1: first one
 * @s2: second one
 * Return: integer contains 0 if mathced
 */
int _strcmp(char *s1, char *s2)
{
	int n;

	n = s1[0] - s2[0];
	return (n);
}
