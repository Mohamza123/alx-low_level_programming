#include "main.h"
/**
 * print_name - prints name
 * @name: the name to print
 * (*f)(char *): the function that prints
 */
void print_name(char *name, (void*)(char *))
{
	void *ptr = printf(char *);
	print_name(name, ptr(name));
}
